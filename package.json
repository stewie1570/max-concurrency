{
  "name": "max-concurrency",
  "version": "1.3.10",
  "description": "Similar to Promise.all() but limits the number of promises in flight.",
  "repository": {
    "type": "git",
    "url": "https://github.com/stewie1570/max-concurrency"
  },
  "main": "lib/index.js",
  "scripts": {
    "test": "jest --coverage --modulePathIgnorePatterns=lib/",
    "tdd": "jest --watch",
    "build": "yarn run test && del-cli ./lib && tsc",
    "prepublishOnly": "yarn run build"
  },
  "keywords": [
    "async",
    "concurrency",
    "limit"
  ],
  "author": "Stewart Anderson",
  "license": "GPL-3.0",
  "devDependencies": {
    "@babel/core": "^7.6.2",
    "@babel/preset-env": "^7.6.2",
    "@babel/preset-typescript": "^7.6.0",
    "@types/jest": "^24.0.18",
    "@types/lodash": "^4.14.141",
    "babel-jest": "^24.9.0",
    "del-cli": "^3.0.0",
    "jest": "^24.9.0",
    "typescript": "^3.6.3"
  },
  "dependencies": {
    "lodash": "^4.17.15"
  },
  "files": [
    "lib"
  ]
}
